# C++ objects and libs
*.o
*.a
*.lib
*.so
*.dylib
*.dll

# Qt Temporary Files
moc_*.cpp
ui_*.h
qrc_*.cpp
Makefile*
*build-*
*.pro.user
*.pro.user.*

# Mac System Files
.DS_Store

# Executables
*.app
*.exe

.qtcreator/
build/

# Bundled Windows MinGW-w64 toolchain -- too large for git, fetched/placed
# manually per windows/README.md, not tracked as source.
windows/mingw64/

# Vendored Monaco Editor build -- too large for git, fetched via
# third_party/fetch-monaco.sh, not tracked as source. (monaco-host/, the
# small bridge page that loads it, *is* tracked -- only this vendored part
# is gitignored.)
third_party/monaco/

# Real Google OAuth client secret -- GitHub's push protection blocks commits
# containing it. Copy secrets.h.example (tracked) to secrets.h and fill in
# the real value locally; config.h includes it.
secrets.h
